bot:
  token: ${BOT_TOKEN}

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/english_bot
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true